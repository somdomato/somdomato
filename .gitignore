# Build artifacts
/tmp/
/tailwindcss
# web/static/css/output.css é versionado com um placeholder (go:embed exige
# que o arquivo exista) — `make css` o sobrescreve com o build real.

# Go
*.test
*.out

# Ansible
ansible/*.retry

# Env — .env.production deve ser criptografado com `ansible-vault encrypt`
# antes de sair da sua máquina; nunca commitar nenhum dos dois em texto plano.
.env
.env.production

# Editor/OS
.DS_Store
.idea/
.vscode/
