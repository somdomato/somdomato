# Build artifacts
/tmp/
/tailwindcss
# web/static/css/output.css é versionado com um placeholder (go:embed exige
# que o arquivo exista) — `make css` o sobrescreve com o build real.

# Go
*.test
*.out

# Ansible
ansible/*.retry

# Ambiente — .env contém segredos e nunca deve ser versionado.
.env

# Editor/OS
.DS_Store
.idea/
.vscode/
cleanupjingles
seed